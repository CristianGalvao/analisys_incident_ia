apiVersion: v1
kind: Service
metadata:
  name: analisys-ai-engine-service
  namespace: analisys-incident-api
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: analisys-ai-engine